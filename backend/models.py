# models.py — NOT USED
#
# This project uses Supabase (Postgres) instead of SQLAlchemy ORM.
# Database schema is defined in:
#   backend/migrations/001_create_schema.sql
#
# All DB access uses the supabase-py client via database.py
